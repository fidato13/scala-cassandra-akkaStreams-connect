package com.lunatech.example

object abc {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(90); 
  val d = List[String]("a","b", "c", "d", null);System.out.println("""d  : List[String] = """ + $show(d ))}
}
